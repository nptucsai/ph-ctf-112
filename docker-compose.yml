version: '2'
services:
  what_does_the_fox_say:
    build:
      dockerfile: ./Dockerfile
      context: ./what_does_the_fox_say
      args:
        - FLAG=0000000000
        - FLAG_FILE=flag.txt
    ports:
      - 10001:80
  sql_injection:
    build:
      dockerfile: ./Dockerfile
      context: ./sql_injection
    environment:
      - FLAG=0000000000
    ports:
      - 10002:80
    volumes:
      - ./sql_injection:/var/www/html
  ph-shop:
    build:
      dockerfile: ./Dockerfile
      context: ./ph-shop
    environment:
      - FLAG=000000000
    ports:
      - 10003:3000
  ssti:
    build:
      dockerfile: ./Dockerfile
      context: ./ssti
      args:
        - FLAG=000000
        - FLAG_FILE=flag.txt
    ports:
      - 10004:8080
    user: 1023:1023
  db:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=password
    volumes:
      - ./data:/var/lib/mysql
